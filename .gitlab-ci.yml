stages:
  - lint
  - build
  - deploy

variables:
  DEPLOY_PATH: /home/team_user/frontend

# 1. Линтинг 
lint:
  stage: lint
  image: node:20-alpine
  script:
    - npm ci
    - npm run lint
    - npx tsc --noEmit
  only:
    - merge_requests
    - main
    - develop

# 2. Сборка 
build:
  stage: build
  image: node:20-alpine
  script:
    - npm ci
    - npm run build
  artifacts:
    paths:
      - dist/
    expire_in: 1 hour
  only:
    - main
    - develop

deploy:
  stage: deploy
  image: alpine:latest
  dependencies:
    - build
  before_script:
    - apk add --no-cache openssh-client rsync
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh && chmod 700 ~/.ssh
    - ssh-keyscan -H $DEPLOY_SERVER >> ~/.ssh/known_hosts
  script:
    # Создаём директорию (на всякий случай)
    - ssh team_user@$DEPLOY_SERVER "mkdir -p $DEPLOY_PATH"
    
    # Копируем файлы
    - rsync -avz --delete dist/ team_user@$DEPLOY_SERVER:$DEPLOY_PATH/
    
    # Останавливаем старый сервер (если был)
    - ssh team_user@$DEPLOY_SERVER "pkill -f 'python3 -m http.server' || true"
    
    - ssh team_user@$DEPLOY_SERVER "cd $DEPLOY_PATH && python3 -m http.server 8080 &> /dev/null & disown"
    
  only:
    - main
    - develop